# Weekly snapshot -- 2026-07-20

Cloud routine run (Monday). Network blocked: cloud egress IP returned TCP failure (exit
code 56) for all outbound URLs including control checks (google.com, cloudflare.com,
dol.gov). All network-dependent tasks are flagged below. See Red Flags.

## Health
- All key pages 200: INCONCLUSIVE (network blocked)
- Pages flagged: none confirmed -- cloud IP blocked, cannot reach site or control URLs
- State page rotation this week (5 states, alphabetical): SD, TN, TX, UT, VT
  (health check skipped -- network blocked)

## Performance
- Not measured: PageSpeed API unreachable (network blocked)

## Em-dash and AI-tell scrub
- Em-dashes: 0 found across src/ and public/posts/
- AI-tell phrases: "leverage" appears 6 times in blog-post-15-negotiate-severance.html
  but all are legitimate negotiation-context usage ("your leverage", "sources of leverage"),
  not AI-tell filler. No rewrite needed.

## Site stats (from this run's audits)
- States with confirmed-current data: 0 verified this run (primary sources unreachable)
- Field notes spot-checked: 0 (source URL verification blocked by network)
- Outbound links audited: 0 (network blocked -- all curl returns exit code 56)
- Schema validation: FAQ JSON-LD and Article JSON-LD confirmed present in state page
  template (src/app/[state]/page.tsx lines 473-533). All 50 state pages covered.
- Internal links: all valid. No broken internal routes found.

## Blog post data consistency check (local, no network needed)
Comparing src/data/blog.ts claims against src/data/states.ts (authoritative data file):

DISCREPANCY 1 -- Louisiana weekly benefit
  Blog (blog.ts line ~138): "$275/week"
  States.ts: $282/week
  
DISCREPANCY 2 -- Massachusetts duration  
  Blog (blog.ts line ~123): "26 weeks"
  States.ts: 30 weeks

DISCREPANCY 3 -- Minnesota waiting week
  Blog (blog.ts line ~127): "no waiting week"
  States.ts: waitingWeek: true

DISCREPANCY 4 -- Texas waiting week
  Blog (blog.ts line ~156): "States without a waiting week (TX, NY, CT, and others)"
  States.ts: waitingWeek: true for TX

All four need Patrick to verify against primary sources and update the blog post to match.
See Red Flags section.

## Changes shipped this week
- FAQ expansion: added "Who qualifies?" and "When should I apply?" questions to Idaho,
  Iowa, and Kansas (3 states, first in the [P] priority queue). All answers pulled from
  existing state data in states.ts, no fabrication. 24 lines added to states.ts.
- 0 blog drafts (network blocked; news scan impossible; no stats verifiable)
- 0 career candidates added
- 0 state benefit figures changed (cannot verify primary sources without network)

## Trend notes
- Network blockage is now the limiting factor for cloud runs. Three consecutive cloud runs
  have had partial or full network failures. Local runs (Patrick) can do the verification
  tasks; cloud runs can handle local-only work (scrubs, FAQ expansion from existing data,
  snapshots, internal consistency checks).
- Local consistency check caught 4 discrepancies between blog.ts and states.ts. Patrick
  should verify Louisiana ($275 vs $282), Massachusetts (26 vs 30 weeks), and TX/MN
  waiting weeks against primary sources, then update the blog post.
- July reindex season: WA/OR/RI were updated 2026-07-11. Other possible July indexers:
  Hawaii (reindexes annually -- current $868 should be verified), Connecticut (Oct typically,
  but worth confirming), NJ (Jan typically). Recommend Patrick verify HI against
  hawaii.gov when online.
- State FAQ expansion on track: Idaho, Iowa, Kansas done this run. Louisiana, Maine,
  Maryland next (verify primary sources before adding).
